<?php
require '../cors.php';  // Enable CORS if needed
header('Content-Type: application/json');

// Sample data for card
$card = [
    "title" => "Welcome to My App",
    "subtitle" => "Your personalized dashboard"
];

// Return JSON response
echo json_encode($card);
?>
